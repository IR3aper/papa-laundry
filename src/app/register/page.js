import RegistrationForm from "@/app/component/custom/RegistrationForm"

export default function Register() {
    return(
<main className="flex flex-col items-center justify-center w-full min-h-screen">
    <RegistrationForm/>
    </main>
    )
}